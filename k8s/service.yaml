apiVersion: v1
kind: Service
metadata:
  name: concordia-backend
  labels:
    app: concordia-backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: concordia-backend