services:
  - type: web
    name: concordia-backend-web
    env: docker
    dockerfilePath: Dockerfile
    buildCommand: npm run build
    startCommand: npm run start
    healthCheckPath: /health
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
